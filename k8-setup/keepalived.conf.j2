vrrp_instance VI_1 {
    state BACKUP
    interface {{ interface }}
    virtual_router_id 51
    priority {{ priority | default(100) }}
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass 1234
    }
    virtual_ipaddress {
        {{ vip }}
    }
}

